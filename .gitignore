# Private files
# The following files contain your database credentials and other personal data.

config/settings.*.php

# Cache, temp and generated files
# The following files are generated by PrestaShop.

admin-dev/autoupgrade/
/cache/*
!/cache/index.php
!/cache/*/
/cache/*/*
!/cache/cachefs/index.php
!/cache/purifier/index.php
!/cache/push/index.php
!/cache/sandbox/index.php
!/cache/smarty/index.php
!/cache/tcpdf/index.php
config/xml/*.xml
/log/*
*sitemap.xml
themes/*/cache/
modules/*/config*.xml

# Site content
# The following folders contain product images, virtual products, CSV's, etc.

admin-dev/backups/
admin-dev/export/
admin-dev/import/
download/
/img/*
upload/

####### Nuevas ignores #######################

# Caché, temperatura y archivos personales

/ .htaccess
* .log
npm-debug.log. * *
.sass-cache /
/ caché / *
descargar / *
/ img / *
/ log / *
/ upload / *
vendedor / *
docs / phpdoc-sf /
compositor.lock
pruebas / Selenium / errorShots /
pruebas / selenio / volcados de error /

/ admin * / autoupgrade / *
/ admin * / copias de seguridad / *
/ admin * / import / *

/ admin * / import / *
! / admin * / import / .htaccess
! / admin * / import / index.php

temas / * / caché / *

# Config

config / settings.inc.php
config / settings.old.php
config / xml / *
config / themes / *
! config / xml / themes / default.xml
tests / Selenium / settings.js
themes / * / config / settings_ * .json
app / config / parameter.old.yml
app / config / config.php

# Temas, módulos y anulaciones

! módulos / *
módulos / config_ * .xml
módulos / ps_imageslider / images / *
! modules / ps_imageslider / images / index.php
módulos / ps_banner / img / *
! modules / ps_banner / img / index.php

! anular / *
/ themes / StarterTheme

# Proveedores y dependencias

bower_components /
node_modules /
compositor.phar
php-cs-fixer
.grunt / *

# Traducciones y plantillas de correos electrónicos

traducciones / *
correos / *
! correos / fr /
themes / default-bootstrap / lang / *
themes / default-bootstrap / modules / * / translation / * .php
themes / default-bootstrap / mails / *
! themes / default-bootstrap / mails / fr /
themes / default-bootstrap / modules / * / mails / *
! themes / default-bootstrap / modules / * / mails / fr

# MISC

* sitemap.xml
/ robots.txt

# Symfony

/ app / bootstrap.php.cache
/ app / cache / *
! aplicación / caché / .gitkeep
/ aplicación / Recursos / traducciones / *
! / app / Resources / translation / default
/ app / test / cache / *
! aplicación / prueba / caché / .gitkeep
/ app / config / parámetros.yml
/ app / config / parameters.php
/ app / logs / *
! app / logs / .gitkeep
/ app / phpunit.xml
/ bin /
/ web / bundles /
/ web / css /
/ web / js /
/ var /


# Creado por https://www.gitignore.io/api/linux,osx,windows,sublimetext,phpstorm

# ## Linux ###
* ~

# Preferencias de directorio de KDE
.directorio

# Carpeta de basura de Linux que puede aparecer en cualquier partición o disco
.Trash- *


# ## OSX ###
.DS_Store
.AppleDouble
.LSOverride

# El icono debe terminar con dos \ r
Icono


# Miniaturas
._ *

# Archivos que pueden aparecer en la raíz de un volumen
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
Artículos temporales
.Trashes
.VolumeIcon.icns

# Directorios potencialmente creados en el recurso compartido AFP remoto
.AppleDB
.AppleDesktop
 Carpeta de basura de  red
 Artículos temporales
.apdisk


# ## Windows ###
# Cachés de archivos de imagen de Windows
Thumbs.db
ehthumbs.db

# Archivo de configuración de carpeta
Desktop.ini

# Papelera de reciclaje utilizada en archivos compartidos
$ RECYCLE.BIN /

# Archivos de Windows Installer
* .cab
* .msi
* .msm
* .msp

# Atajos de Windows
* .lnk


# ## SublimeText ###
# archivos de caché para texto sublime
* .tmlanguage.cache
* .tmPreferences.cache
* .stTheme.cache

# los archivos del espacio de trabajo son específicos del usuario
* .sublime-workspace

# los archivos del proyecto deben registrarse en el repositorio, a menos que se
# proporción de contribuyentes probablemente no usará SublimeText
# * .sublime-project

# archivo de configuración sftp
sftp-config.json


# ## PhpStorm ###
# Cubre IDE de JetBrains: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio

* .iml

# # Formato de proyecto basado en directorio:
.idea /

# # Formato de proyecto basado en archivo:
* .ipr
* .iws

# # Archivos específicos del complemento:

# IntelliJ
/ fuera /

# mpeltonen / sbt-idea plugin
.idea_modules /

# Complemento JIRA
atlassian-ide-plugin.xml

# Complemento Crashlytics (para Android Studio e IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties

# ## NetBeans ###
nbproject / private /
construir /
nbbuild /
dist /
nbdist /
nbactions.xml
nb-configuration.xml
.nb-gradle /

# ## Eclipse ###
.Construir camino
.proyecto
.settings /
.externalToolBuilders /
